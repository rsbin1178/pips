// Module fence: this directory holds cc-skills-golang skill assets (docs and
// example snippets), not code belonging to github.com/rsbin/pips. The go.mod
// here excludes the subtree from the parent module's ./... walks.
module skills.invalid

go 1.26
